# The one config file: holds the session secret and, on a workstation, provider
# credentials. Only the example is committed.
/naaf.conf
.env
.env.*
*.db
*.db-shm
*.db-wal
/tmp/
/log/
*.key
*.conf.generated

# deploy: local tee of each ./deploy.sh run
deploy/logs/

# editor / tooling caches
.DS_Store
.ruby-lsp/

# Claude Code — personal/local state
.claude/settings.local.json
.claude/.credentials.json
.claude/projects/
.claude/shell-snapshots/
.claude/todos/
.claude/statsig/
.claude/history.jsonl
.claude/ide/
